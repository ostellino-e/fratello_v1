# Fratello v3.2.2 — Navegación corregida

- Las tarjetas de Inicio vuelven a abrir sus secciones.
- Se eliminó la doble captura `pointerup` y `click`.
- Toda la navegación usa `data-seccion`.
- Los botones Volver usan `data-volver`.
- Se mantienen Firebase online, pedidos externos y notificaciones.
